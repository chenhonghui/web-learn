

```javascript
/**
 * @param {number[]} nums
 * @return {number}
 */
var singleNumber = function(nums) {
   return nums.reduce((res,cur) => {
       const keyname = cur+"";
      if( res[keyname] ){
          res.singleNumbe = res.singleNumber.replace(keyname,'');
      }else{
          res[keyname]=1;
          res.singleNumber += keyname;
      }
      return res;
  },{singleNumber:''}).singleNumber;
};
```

